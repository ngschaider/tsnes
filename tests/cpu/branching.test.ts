describe("CPU - BRANCHING", () => {
    test("0x00 - BRK (IMM)", () => {

    });
    
    test("0x10 - BPL (REL)", () => {
    
    });
    
    test("0x20 - JSR (ABS)", () => {
    
    });
    
    test("0x24 - BIT (ZP0)", () => {
    
    });

    test("0x2C - BIT (ABS)", () => {
    
    });
    
    test("0x30 - BMI (REL)", () => {
    
    });
    
    test("0x40 - RTI (IMP)", () => {

    });

    test("0x4C - JMP (ABS)", () => {
    
    });
    
    test("0x50 - BVC (REL)", () => {
    
    });

    test("0x60 - RTS (IMP)", () => {

    });  
        
    test("0x6C - JMP (IND)", () => {
    
    });
    
    test("0x70 - BVS (REL)", () => {
    
    });
      
    test("0x90 - BCC (REL)", () => {
    
    });
    
    test("0xB0 - BCS (REL)", () => {
    
    });
    
    test("0xD0 - BNE (REL)", () => {
    
    });

    test("0xF0 - BEQ (REL)", () => {
    
    });
    
});